%nav.navbar
  -if current_user
    =link_to 'Users', root_path, class:'navlink'
    =link_to 'Matches', matches_path(current_user.id), class:'navlink'
    =link_to 'Questions', questions_path(current_user.id), class:'navlink'
  -else
    =link_to 'Sign In', new_user_session_path, class:'navlink center'
    =link_to 'Sign Up', new_user_registration_path, class:'navlink'

  
  